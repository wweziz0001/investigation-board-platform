# v2.0.8

## Bug Fixes

### Fixed Event Save Error
- **Issue**: TypeError when saving event - "Cannot read properties of undefined (reading 'eventType')"
- **Cause**: events array contained undefined elements when mapping to get event types
- **Solution**: Added `.filter(Boolean)` before `.map()` in the eventTypes useMemo hook

### Files Changed
- `src/components/board/investigation-board.tsx` - Added filter(Boolean) to eventTypes calculation
